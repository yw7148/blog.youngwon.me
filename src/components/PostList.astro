---
import PostCard from './PostCard.astro';
import type { Post } from '../lib/posts';

interface Props {
  posts: Post[];
  emptyMessage?: string;
}

const { posts, emptyMessage = '표시할 글이 없습니다.' } = Astro.props;
---

{
  posts.length > 0 ? (
    <div class="post-list">
      {posts.map((post) => <PostCard post={post} />)}
    </div>
  ) : (
    <p class="empty-state">{emptyMessage}</p>
  )
}
